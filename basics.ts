const add = (n1: number, n2: number) => {
  // assigning types to each parameter, done after each parameter using a colon
  return n1 + n2;
};

const number1 = 5;
const number2 = 2.8;

const result = add(number1, number2);
console.log(result);
